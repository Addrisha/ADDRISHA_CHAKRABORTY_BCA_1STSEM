#include<stdio.h>
int main(){
	// checking the number is divisible by 2 or not
	//if the result is 1 then it is divisible by 2
	//if the result is 0 then it is not divisible by 2
	int num;
	printf("enter a num ");
	scanf("%d",&num);
	printf("%d",num%2==0);
	}
